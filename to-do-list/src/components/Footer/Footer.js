import React from 'react';
import classes from './Footer.css';

const footer = (props) => (
    <div className="Footer">
        <p className="Id">ID Seleccionado:</p>
        <p className="Users">Número de usuarios:</p>
    </div>
);

export default footer;